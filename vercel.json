{
    "functions": {
        "api/**/*.{js,ts}": {
            "runtime": "nodejs",
            "includeFiles": "private_docs/**"
        }
    },
    "routes": [
        { "src": "/api/(.*)", "dest": "/api/$1" }
    ]
}
